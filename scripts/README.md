# Scripts Directory

This directory contains utility scripts for VoiceScribe Studio.

## Available Scripts

- `start.sh` - Shell script to start the VoiceScribe Studio application

## Usage

To run scripts from this directory:

```bash
# From project root
bash ./scripts/start.sh

# OR use the compatibility script in the root directory
bash ./start.sh
```
